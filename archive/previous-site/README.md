# Versione precedente

Questa cartella conserva la versione del sito precedente al redesign mobile con animazioni legate allo scroll.

File archiviati:

- `main.tsx`: applicazione React precedente
- `styles.css`: stile precedente
- `index.html`: metadati e shell HTML precedenti

Gli asset condivisi restano nella cartella `public/` alla radice del progetto.
