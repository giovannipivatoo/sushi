# This key is embedded in the browser bundle. Restrict it to your GitHub Pages URL
# in Google Cloud Console and enable Maps JavaScript API + Places API (New).
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key
